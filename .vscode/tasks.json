{
  "version": "2.0.0",
  "tasks": [
    {
      "type": "shell",
      "label": "Make Debug",
      "command": "cmake -DCMAKE_BUILD_TYPE=debug .. && make",
      "options": {
         "cwd": "${workspaceFolder}/build",
      },
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}